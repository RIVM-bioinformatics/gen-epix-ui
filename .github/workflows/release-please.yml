name: release-please
on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      ui-client-common-release-created: ${{ steps.release.outputs['packages/ui-client-common--release_created'] }}
      ui-client-common-tag-name: ${{ steps.release.outputs['packages/ui-client-common--tag_name'] }}
      ui-core-release-created: ${{ steps.release.outputs['packages/ui-core--release_created'] }}
      ui-core-tag-name: ${{ steps.release.outputs['packages/ui-core--tag_name'] }}
      ui-core-components-release-created: ${{ steps.release.outputs['packages/ui-core-components--release_created'] }}
      ui-core-components-tag-name: ${{ steps.release.outputs['packages/ui-core-components--tag_name'] }}
      ui-casedb-release-created: ${{ steps.release.outputs['packages/ui-casedb--release_created'] }}
      ui-casedb-tag-name: ${{ steps.release.outputs['packages/ui-casedb--tag_name'] }}
      ui-omopdb-release-created: ${{ steps.release.outputs['packages/ui-omopdb--release_created'] }}
      ui-omopdb-tag-name: ${{ steps.release.outputs['packages/ui-omopdb--tag_name'] }}
      ui-seqdb-release-created: ${{ steps.release.outputs['packages/ui-seqdb--release_created'] }}
      ui-seqdb-tag-name: ${{ steps.release.outputs['packages/ui-seqdb--tag_name'] }}
      ui-phylogenetic-tree-release-created: ${{ steps.release.outputs['packages/ui-phylogenetic-tree--release_created'] }}
      ui-phylogenetic-tree-tag-name: ${{ steps.release.outputs['packages/ui-phylogenetic-tree--tag_name'] }}
      ui-core-form-release-created: ${{ steps.release.outputs['packages/ui-core-form--release_created'] }}
      ui-core-form-tag-name: ${{ steps.release.outputs['packages/ui-core-form--tag_name'] }}
      api-casedb-release-created: ${{ steps.release.outputs['packages/api-casedb--release_created'] }}
      api-casedb-tag-name: ${{ steps.release.outputs['packages/api-casedb--tag_name'] }}
      api-commondb-release-created: ${{ steps.release.outputs['packages/api-commondb--release_created'] }}
      api-commondb-tag-name: ${{ steps.release.outputs['packages/api-commondb--tag_name'] }}
      api-seqdb-release-created: ${{ steps.release.outputs['packages/api-seqdb--release_created'] }}
      api-seqdb-tag-name: ${{ steps.release.outputs['packages/api-seqdb--tag_name'] }}
      api-omopdb-release-created: ${{ steps.release.outputs['packages/api-omopdb--release_created'] }}
      api-omopdb-tag-name: ${{ steps.release.outputs['packages/api-omopdb--tag_name'] }}
    steps:
      - uses: googleapis/release-please-action@v4.2.0
        id: release
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

  publish-ui-casedb:
    needs: release-please
    if: ${{ needs.release-please.outputs.ui-casedb-release-created == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-node-env
        with:
          node-version: 24

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create .npmrc
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build
        run: pnpm --filter @gen-epix/ui-casedb run build

      - name: Publish to NPM
        run: pnpm --filter @gen-epix/ui-casedb publish --access=public --no-git-checks

      - name: Zip dist directory
        run: zip -r gen-epix-ui-casedb.zip packages/ui-casedb/dist

      - name: Upload release artifact to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ needs.release-please.outputs.ui-casedb-tag-name }} gen-epix-ui-casedb.zip

  publish-ui-omopdb:
    needs: release-please
    if: ${{ needs.release-please.outputs.ui-omopdb-release-created == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-node-env
        with:
          node-version: 24

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create .npmrc
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build
        run: pnpm --filter @gen-epix/ui-omopdb run build

      - name: Publish to NPM
        run: pnpm --filter @gen-epix/ui-omopdb publish --access=public --no-git-checks

      - name: Zip dist directory
        run: zip -r gen-epix-ui-omopdb.zip packages/ui-omopdb/dist

      - name: Upload release artifact to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ needs.release-please.outputs.ui-omopdb-tag-name }} gen-epix-ui-omopdb.zip

  publish-ui-seqdb:
    needs: release-please
    if: ${{ needs.release-please.outputs.ui-seqdb-release-created == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-node-env
        with:
          node-version: 24

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create .npmrc
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build
        run: pnpm --filter @gen-epix/ui-seqdb run build

      - name: Publish to NPM
        run: pnpm --filter @gen-epix/ui-seqdb publish --access=public --no-git-checks

      - name: Zip dist directory
        run: zip -r gen-epix-ui-seqdb.zip packages/ui-seqdb/dist

      - name: Upload release artifact to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ needs.release-please.outputs.ui-seqdb-tag-name }} gen-epix-ui-seqdb.zip

  publish-ui-client-common:
    needs: release-please
    if: ${{ needs.release-please.outputs.ui-client-common-release-created == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-node-env
        with:
          node-version: 24

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create .npmrc
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build
        run: pnpm --filter @gen-epix/ui-client-common run build

      - name: Publish to NPM
        run: pnpm --filter @gen-epix/ui-client-common publish --access=public --no-git-checks

      - name: Zip dist directory
        run: zip -r gen-epix-ui-client-common.zip packages/ui-client-common/dist

      - name: Upload release artifact to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ needs.release-please.outputs.ui-client-common-tag-name }} gen-epix-ui-client-common.zip

  publish-ui-phylogenetic-tree:
    needs: release-please
    if: ${{ needs.release-please.outputs.ui-phylogenetic-tree-release-created == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-node-env
        with:
          node-version: 24

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create .npmrc
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build
        run: pnpm --filter @gen-epix/ui-phylogenetic-tree run build

      - name: Publish to NPM
        run: pnpm --filter @gen-epix/ui-phylogenetic-tree publish --access=public --no-git-checks

      - name: Zip dist directory
        run: zip -r gen-epix-ui-phylogenetic-tree.zip packages/ui-phylogenetic-tree/dist

      - name: Upload release artifact to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ needs.release-please.outputs.ui-phylogenetic-tree-tag-name }} gen-epix-ui-phylogenetic-tree.zip

  publish-ui-core:
    needs: release-please
    if: ${{ needs.release-please.outputs.ui-core-release-created == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-node-env
        with:
          node-version: 24

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create .npmrc
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build
        run: pnpm --filter @gen-epix/ui-core run build

      - name: Publish to NPM
        run: pnpm --filter @gen-epix/ui-core publish --access=public --no-git-checks

      - name: Zip dist directory
        run: zip -r gen-epix-ui-core.zip packages/ui-core/dist

      - name: Upload release artifact to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ needs.release-please.outputs.ui-core-tag-name }} gen-epix-ui-core.zip

  publish-ui-core-components:
    needs: release-please
    if: ${{ needs.release-please.outputs.ui-core-components-release-created == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-node-env
        with:
          node-version: 24

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create .npmrc
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build
        run: pnpm --filter @gen-epix/ui-core-components run build

      - name: Publish to NPM
        run: pnpm --filter @gen-epix/ui-core-components publish --access=public --no-git-checks

      - name: Zip dist directory
        run: zip -r gen-epix-ui-core-components.zip packages/ui-core-components/dist

      - name: Upload release artifact to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ needs.release-please.outputs.ui-core-components-tag-name }} gen-epix-ui-core-components.zip

  publish-api-casedb:
    needs: release-please
    if: ${{ needs.release-please.outputs.api-casedb-release-created == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-node-env
        with:
          node-version: 24

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create .npmrc
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build
        run: pnpm --filter @gen-epix/api-casedb run build

      - name: Publish to NPM
        run: pnpm --filter @gen-epix/api-casedb publish --access=public --no-git-checks

      - name: Zip dist directory
        run: zip -r gen-epix-api-casedb.zip packages/api-casedb/dist

      - name: Upload release artifact to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ needs.release-please.outputs.api-casedb-tag-name }} gen-epix-api-casedb.zip

  publish-api-commondb:
    needs: release-please
    if: ${{ needs.release-please.outputs.api-commondb-release-created == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-node-env
        with:
          node-version: 24

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create .npmrc
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build
        run: pnpm --filter @gen-epix/api-commondb run build

      - name: Publish to NPM
        run: pnpm --filter @gen-epix/api-commondb publish --access=public --no-git-checks

      - name: Zip dist directory
        run: zip -r gen-epix-api-commondb.zip packages/api-commondb/dist

      - name: Upload release artifact to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ needs.release-please.outputs.api-commondb-tag-name }} gen-epix-api-commondb.zip

  publish-api-seqdb:
    needs: release-please
    if: ${{ needs.release-please.outputs.api-seqdb-release-created == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-node-env
        with:
          node-version: 24

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create .npmrc
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build
        run: pnpm --filter @gen-epix/api-seqdb run build

      - name: Publish to NPM
        run: pnpm --filter @gen-epix/api-seqdb publish --access=public --no-git-checks

      - name: Zip dist directory
        run: zip -r gen-epix-api-seqdb.zip packages/api-seqdb/dist

      - name: Upload release artifact to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ needs.release-please.outputs.api-seqdb-tag-name }} gen-epix-api-seqdb.zip

  publish-api-omopdb:
    needs: release-please
    if: ${{ needs.release-please.outputs.api-omopdb-release-created == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-node-env
        with:
          node-version: 24

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create .npmrc
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build
        run: pnpm --filter @gen-epix/api-omopdb run build

      - name: Publish to NPM
        run: pnpm --filter @gen-epix/api-omopdb publish --access=public --no-git-checks

      - name: Zip dist directory
        run: zip -r gen-epix-api-omopdb.zip packages/api-omopdb/dist

      - name: Upload release artifact to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ needs.release-please.outputs.api-omopdb-tag-name }} gen-epix-api-omopdb.zip

  publish-ui-core-form:
    needs: release-please
    if: ${{ needs.release-please.outputs.ui-core-form-release-created == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/setup-node-env
        with:
          node-version: 24

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create .npmrc
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build
        run: pnpm --filter @gen-epix/ui-core-form run build

      - name: Publish to NPM
        run: pnpm --filter @gen-epix/ui-core-form publish --access=public --no-git-checks

      - name: Zip dist directory
        run: zip -r gen-epix-ui-core-form.zip packages/ui-core-form/dist

      - name: Upload release artifact to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ needs.release-please.outputs.ui-core-form-tag-name }} gen-epix-ui-core-form.zip
