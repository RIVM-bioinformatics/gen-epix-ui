export interface GenEpixUiTheme {
  'gen-epix-ui': {
    footer: {
      background: string;
      color: string;
      sectionBorderColor: string;
    };
    navbar: {
      activeBackground: string;
      activeColor: string;
      background: string;
      environmentMessageColor: string;
      primaryColor: string;
      secondaryColor: string;
    };
  };
}
